> **Referenz.** Datierter T-501-Auditbeleg; die lebende Zusammenfassung steht in `docs/WEB_UI_API_TOOL_RESET_STATUS.md`.

# T-501 Audit – 2026-09-06

## Matrixstand

Die Matrix enthält 130 Zellen:

| Status | Anzahl |
|---|---:|
| `passed` | 109 |
| `unsupported` | 10 |
| `unreachable` | 4 |
| `not_run` | 7 |
| `failed` | 0 |

Die Bereiche `webui_chat`, `streaming`, `attachment`, `api_models`, `api_chat`, `api_responses`, `health`, `sources`, `groups` und `security` stehen jeweils bei 10/10 `passed`.

## Managed Tools

Die zehn `managed_tools`-Zellen wurden von `failed` auf `unsupported` korrigiert. Der Beleg `managed_tools_rejected_2026-09-04.json` dokumentiert den erwarteten HTTP-400-Fehler: Aktive Client-Function-Tools werden im clean browser text profile bewusst nicht in die Browser-Unterhaltung injiziert. Das ist eine dokumentierte Capability-Grenze und kein zufälliger Provider- oder Bridge-Fehler. Die verwalteten WebAgent-Tools folgen separat über den Managed-Agent-Pfad.

## Offen

Offen bleiben sieben `effort`-Zellen und vier `model`-Zellen. Sie sind derzeit als `not_run` beziehungsweise `unreachable` klassifiziert, weil die benötigten Browser-UI-Pfade, Logins oder Provider-Zustände nicht belastbar verfügbar waren. T-501 wird deshalb nicht auf `done` gesetzt.

## Verifikation

`cargo test --lib` wurde auf dem Branch mit **1316 passed / 0 failed / 1 ignored** ausgeführt. JSON-Syntax und `git diff --check` waren ebenfalls grün.
